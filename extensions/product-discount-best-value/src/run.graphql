query RunInput {
  cart {
    lines {
      id
      quantity

      _count: attribute(key: "_count") {
        value
      }

      merchandise {
        __typename
        ... on ProductVariant {
          id
          product {
            productType
          }
        }
      }
    }
  }
}
